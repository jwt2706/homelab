# Plain HTTP is intentional — these names only resolve inside your tailnet via
# Split DNS, so there's no public hostname for Caddy to get real ACME certs for.

http://jellyfin.home {
	reverse_proxy localhost:8096
}

http://couchdb.home {
	reverse_proxy localhost:5984
}